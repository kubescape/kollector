package watch

import (
	"testing"
)

func TestAPIVer(t *testing.T) {
	// conf, err := clientcmd.BuildConfigFromFlags("", "/home/cadev/.kube/config")
	// if err != nil {
	// 	t.Errorf("K8s API version conf: %v", err)

	// }
	// clientset, err := kubernetes.NewForConfig(conf)

	// if err != nil {
	// 	t.Errorf("K8s API version client: %v", err)

	// }
	// serverVersion, err := clientset.Discovery().ServerVersion()
	// t.Errorf("K8s API version :%+v, %v", serverVersion, err)

}
